groups:
  - name: my_rules
    rules:
    - alert: HighRequestLatency
      expr: rate(http_server_requests_seconds_count{uri="/doit"}[10m]) > 10
      for: 1m
      labels:
        severity: high
      annotations:
        application: springbootify